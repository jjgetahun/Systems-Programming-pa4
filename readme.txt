Our index program uses code from our tokenizer and sorted-list to index files. Tokenizer goes through each file we parse and returns a normalized string of tokens (normalized as in eack token is seperated by one white space). This takes linear time to scan through each file once. Our sorted list 

